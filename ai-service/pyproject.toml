[build-system]
requires = ["setuptools==80.3.1", "wheel==0.45.1"]
build-backend = "setuptools.build_meta"

[project]
name = "serviceops-ai"
version = "0.2.0"
description = "Deterministic ServiceOps prediction and cited knowledge service"
requires-python = ">=3.12,<3.14"
dependencies = [
    "confluent-kafka==2.8.2",
    "fastapi==0.115.12",
    "joblib==1.4.2",
    "jsonschema==4.23.0",
    "opentelemetry-api==1.42.1",
    "opentelemetry-distro==0.63b1",
    "opentelemetry-exporter-otlp-proto-http==1.42.1",
    "opentelemetry-instrumentation-confluent-kafka==0.63b1",
    "opentelemetry-instrumentation-fastapi==0.63b1",
    "opentelemetry-instrumentation-logging==0.63b1",
    "pandas==2.2.3",
    "prometheus-client==0.22.1",
    "pydantic==2.11.4",
    "PyJWT==2.13.0",
    "scikit-learn==1.6.1",
    "uvicorn[standard]==0.34.2",
]

[project.scripts]
serviceops-generate-training-data = "serviceops_ai.dataset:main"

[project.optional-dependencies]
dev = [
    "httpx==0.28.1",
    "pytest==8.3.5",
    "ruff==0.11.8",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
