# Azure deployment

This Terraform root deploys the complete ServiceOps runtime to Azure without adding any
analytics, RAG, observability, or Kubernetes components. It creates:

- a workload-profile Azure Container Apps environment integrated with a dedicated VNet;
- public HTTPS ingress for the React/Nginx frontend and internal ingress for Spring Boot;
- one always-running Spring replica and one always-running Kafka prediction worker;
- Azure Database for PostgreSQL Flexible Server on a delegated private subnet;
- a Standard Event Hubs namespace with three pre-created Kafka-compatible event hubs;
- a Basic Azure Container Registry with administrator credentials disabled; and
- a user-assigned identity with pull-only registry access for all three applications.

The Event Hubs endpoint is TLS protected and the application authorization rule is stored
as Container Apps secrets. PostgreSQL has no public endpoint. Application passwords and
the JWT signing key are generated by Terraform and therefore exist in encrypted remote
state as well as Container Apps secrets. Azure Key Vault and external identity federation
remain separate roadmap work.

## Recommended GitHub deployment

The `Azure deployment` workflow is manual; pushes and pull requests never create Azure
resources. Configure an Entra application with a GitHub OIDC federated credential for the
repository's `main` branch, then add these GitHub Actions secrets:

| Secret | Value |
| --- | --- |
| `AZURE_CLIENT_ID` | Entra application/client ID |
| `AZURE_TENANT_ID` | Entra tenant ID |
| `AZURE_SUBSCRIPTION_ID` | Target subscription ID |
| `AZURE_PRINCIPAL_OBJECT_ID` | Object ID of the service principal, not its client ID |

The service principal needs `Contributor` and `Role Based Access Control Administrator`
on the target subscription. The second role is required only for the pull-only ACR role
and state-storage data role assignments; scope it more narrowly if the destination
resource groups are provisioned in advance.

Add these repository variables:

| Variable | Constraint |
| --- | --- |
| `AZURE_STATE_RESOURCE_GROUP` | Dedicated resource group for Terraform state |
| `AZURE_STATE_STORAGE_ACCOUNT` | Globally unique, 3-24 lowercase alphanumeric name |

Run the workflow from `main` with `action=deploy`. It performs the following guarded
sequence:

1. signs in through OIDC without a stored Azure client secret;
2. creates or reuses the encrypted state account and assigns blob-data access;
3. initializes and validates Terraform;
4. bootstraps ACR if this is the first run;
5. builds all three images remotely in ACR with an immutable commit tag;
6. plans and applies the complete workload; and
7. signs in through the public frontend, creates a ticket, and waits for its Event Hubs
   round trip and ML classification.

The generated operator password is masked in workflow logs. Retrieve it only when needed:

```bash
terraform -chdir=infra/azure output -raw operator_password
```

Treat that output as a secret. Existing bootstrap database accounts are not overwritten
when a password is later regenerated; account rotation must update the persisted account
or recreate an empty demo database.

## Destroy and cost control

Azure Database for PostgreSQL, Event Hubs Standard, ACR, and the two minimum Container Apps
replicas accrue charges until deleted. Run the same workflow with `action=destroy` and enter
the environment name in `confirm_destroy`. Terraform removes all workload resources and
retains only the small shared state resource group and storage account so audit history and
future deployments remain safe.

After every Terraform state stored in that account has been destroyed, the dedicated state
resource group can also be deleted explicitly:

```bash
az group delete --name <AZURE_STATE_RESOURCE_GROUP> --yes
```

Do not delete the state backend while any managed environment still exists.

## Local Terraform validation

Terraform 1.15.8 is pinned in CI and the deployment workflow. Validation does not require
Azure credentials:

```bash
terraform -chdir=infra/azure fmt -check -recursive
terraform -chdir=infra/azure init -backend=false
terraform -chdir=infra/azure validate
terraform -chdir=infra/azure test
```

The mocked plan test asserts private PostgreSQL, internal backend ingress, public frontend
ingress, disabled ACR administrator credentials, three event hubs, and a non-zero AI worker.
An actual plan/apply still requires Azure credentials, remote backend settings, and images
already available in ACR; the workflow owns that bootstrap sequence.

## Service compatibility settings

Event Hubs does not support Kafka `AdminClient` topic management, so Terraform creates the
three event hubs and the cloud backend sets `SERVICEOPS_TOPICS_MANAGE=false`. Spring and
Python receive the required `SASL_SSL`/`PLAIN` connection settings and Event Hubs-specific
timeouts. Local Compose keeps its original plaintext Redpanda behavior.

The frontend image uses Nginx's startup template support. Compose injects
`http://backend:8080`, while Azure injects the backend's internal Container Apps HTTPS
hostname. Only the frontend receives public ingress.

Reference documentation:

- [Azure Container Apps networking](https://learn.microsoft.com/azure/container-apps/networking)
- [Event Hubs Kafka client configuration](https://learn.microsoft.com/azure/event-hubs/apache-kafka-configurations)
- [PostgreSQL Flexible Server private networking](https://learn.microsoft.com/azure/postgresql/flexible-server/concepts-networking-private)
- [Terraform AzureRM provider](https://registry.terraform.io/providers/hashicorp/azurerm/latest/docs)
